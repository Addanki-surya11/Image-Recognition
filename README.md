# Image-Recognition
Built an image classification model using Convolutional Neural Networks (CNNs) in TensorFlow/PyTorch. Trained on datasets like CIFAR-10 and Fashion-MNIST to categorize images into classes with effective accuracy through image preprocessing and model tuning.
